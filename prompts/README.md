# Prompts (PLT-1)
Versioned prompt files, one per agent role, named `<role>.<version>.md`
(e.g. `orchestrator.p1.md`). Referenced by version in approval decision records
(HUM-2 `versions.prompt_version`). First prompts land Day 3.
