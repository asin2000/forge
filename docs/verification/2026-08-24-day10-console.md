# Day-10 console additions — acceptance record (2026-08-24)

Scope: three entrant-directed console additions, all read-side projections
of governed state (no agent code, IAM, or contract changes):

- **Agent Operations lanes** (PR #26, `81e9208`): a box per agent
  definition, live "now" composed from bus claims (unexpired leases),
  ASSIGNED work packages, registry instance state, and the audit trail —
  metadata-only (no prompts, no model prose, no audit `detail`), motion
  event-driven only. Endpoint: `GET /api/agents/now`.
- **Fixed bottom dock** (PR #27, `1ccfc6b`): the wall pins to the viewport
  bottom (the entrant's taskbar concept); below-the-fold at 1080p was a
  recording-resolution defect.
- **Fleet readiness strip + collapsible dock** (PR #28, `18ec4b3`):
  `GET /api/fleet` derives per-vehicle status from workflow state alone
  (live recovery = NMC IN_RECOVERY / BLOCKED; else MISSION_CAPABLE); the
  twelve GX-12 tiles + FLEET READINESS n/12 chip head the page; the dock
  gains Collapse/Expand with an "n working" summary.

Tag **`v1.3-demo.3` = `18ec4b3`**. Suite at candidate: **308 tests
(287 unit + 21 real-client emulator)**, 8/8 CI gates green on every PR.

## CI-8 — 10/10 consecutive live spines (verbatim)

```
== FORGE acceptance: 10 consecutive live spines
   project=forge-agentic-0821-6418
   candidate=18ec4b344be4a35a1780382828a4d8add4b726fc
   [PASS] run 1/10  (68s)
   [PASS] run 2/10  (48s)
   [PASS] run 3/10  (47s)
   [PASS] run 4/10  (51s)
   [PASS] run 5/10  (51s)
   [PASS] run 6/10  (52s)
   [PASS] run 7/10  (52s)
   [PASS] run 8/10  (52s)
   [PASS] run 9/10  (51s)
   [PASS] run 10/10  (46s)
ACCEPTANCE: PASS 10/10 consecutive on 18ec4b344be4a35a1780382828a4d8add4b726fc
```

First attempt, zero resets. Dress rehearsal same day: **PASS, 76 s**
(budget 240 s).

## Live visual verification

Headless captures against the deployed console (operator identity token,
the recording path) during a real console-started spine:

- Mid-spine: maintenance AND supply lanes simultaneously pulsing
  "● Processing work_package_assignment.v2" — parallel execution visible.
- At gate 1: safety lane processing `roster_assignment.v2` while the
  schedule_override card renders; workflow later approved by the entrant
  from the live console.
- Full console: FLEET READINESS 11/12 MC with the NMC tile click-through,
  audit trail center, dock reporting "all quiet" when the fleet idles.

Notably, the entrant drove a full console spine to RELEASED on the
deployed fleet during this window (`wf-op-ca78a410bc`) — the first
end-to-end recovery started, gated, time-skipped, and released entirely
by a human operator through the HUM-3 surface.

State after capture: fleet = `v1.3-demo.3` runtime; the entrant's live
workflows left untouched. Recording precondition (clock to Day 0) runs in
the DEMO_RUNBOOK pre-record step as always.
