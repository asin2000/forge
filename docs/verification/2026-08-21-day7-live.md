# Day 7 live verification — 2026-08-21 (captures span the UTC boundary)

Project `forge-agentic-0821-6418`. Scope: the complete PLT-6 deployment
command converging IDEMPOTENTLY on the standing project (the fresh-project
run is PLT-6's own verify criterion and remains scheduled for Day 8 —
traceability keeps PLT-6's live verification pending until then), the
deployed agent fleet (5 workers
+ dashboard behind Cloud Run IAM), scoped AGT-6 IAM, Cloud Logging
regionalization (DAT-1), live DLQ forwarding (ICD-5/CI-6), and the live
end-to-end spine with a recorded human decision (Lane 2). All output
verbatim from the live project.

## 1. deploy.sh — the single PLT-6 command

`PROJECT_ID=... PYTHON=./.venv/bin/python bash infra/deploy.sh`
chains infra/setup-gcp.sh (APIs + Firestore), then: 7 per-role SAs (429
backoff), SCOPED IAM, bus + DLQ, regional log bucket, registry load, ONE
container build, 5 agent workers + dashboard on Cloud Run
(--no-allow-unauthenticated), filtered ordered PUSH subscriptions (OIDC =
role SA, DLQ after 5). Idempotent — re-run converges.

Production findings found and fixed DURING the live runs (each now encoded
in deploy.sh):
- requirements.lock was missing the OTel Cloud Trace exporter — the first
  worker revision crashed on import (lock regenerated; the lockfile IS the
  deploy contract).
- Pub/Sub FILTER EXPRESSIONS CAP AT 256 CHARS (the orchestrator's 7-schema
  OR filter was 365). Routing now stamps compact `to_<role>` attributes
  from ONE canonical map (forge_common.pubsub.ROUTING) and each
  subscription filters on a single existence check; CI-9 cross-checks the
  map against registry consumes declarations.
- Dead-lettering needs BOTH service-agent grants. With only the
  DLQ-topic publisher half, exhausted messages keep REDELIVERING (observed:
  a poison message redelivered ~20 times); with roles/pubsub.subscriber on
  the source subscription added, forwarding works.
- Span export needs roles/cloudtrace.agent on the service accounts
  (Cloud Trace exporter logged PermissionDenied until granted).

## 2. Cloud Logging regionalized (DAT-1)

```
projects/forge-agentic-0821-6418/locations/us-central1/buckets/forge-logs  ACTIVE  30
_Default sink -> logging.googleapis.com/projects/forge-agentic-0821-6418/locations/us-central1/buckets/forge-logs
```

`_Required` predates (created with the project, location fixed) and holds
only Google admin-activity audit entries — never operational payload data
(disclosed in the README's Data residency section, per DAT-3).

## 3. DAT-1 enforce-in-transit — live refusal (unplanned positive)

A deliberate publish via the GLOBAL Pub/Sub endpoint was refused by the
topic's message storage policy:

```
FAILED_PRECONDITION: The topic's message storage policy requires
enforcement in transit, but the Publish request was received by a Pub/Sub
server in a non-allowed region.
```

Production code publishes via `pubsub.us-central1.rep.googleapis.com` and
is unaffected — the policy blocks exactly the out-of-region path it should.

## 4. DLQ forwarding (ICD-5 / CI-6) — live

A poison payload (undecodable bytes, `to_orchestrator` attribute) was
published via the regional endpoint. The orchestrator worker returned 500
per delivery; after exhaustion Pub/Sub forwarded it to `forge-bus-dlq`:

```
== DLQ pull (1 message(s)):
   data: b'NOT-JSON-POISON-FOR-DLQ-PROOF'
   attributes: {'CloudPubSubDeadLetterSourceDeliveryCount': '6',
    'CloudPubSubDeadLetterSourceSubscriptionProject': 'forge-agentic-0821-6418',
    'schema_version': 'poison.test', 'to_orchestrator': '1',
    'CloudPubSubDeadLetterSourceTopicPublishTime': '2026-08-22T01:15:14.596+00:00',
    'CloudPubSubDeadLetterSourceSubscription': 'forge-bus-orchestrator'}
   acked (DLQ drained)
```


## 5. The deployed fleet + scoped IAM (PLT-3 / AGT-6)

```
== capture 2026-08-22T02:10:45Z — the deployed fleet
NAME                URL                                                 SERVICE_ACCOUNT_NAME
forge-dashboard     https://forge-dashboard-3vnecimj5a-uc.a.run.app     forge-dashboard@forge-agentic-0821-6418.iam.gserviceaccount.com
forge-maintenance   https://forge-maintenance-3vnecimj5a-uc.a.run.app   forge-maintenance@forge-agentic-0821-6418.iam.gserviceaccount.com
forge-orchestrator  https://forge-orchestrator-3vnecimj5a-uc.a.run.app  forge-orchestrator@forge-agentic-0821-6418.iam.gserviceaccount.com
forge-safety        https://forge-safety-3vnecimj5a-uc.a.run.app        forge-safety@forge-agentic-0821-6418.iam.gserviceaccount.com
forge-supply        https://forge-supply-3vnecimj5a-uc.a.run.app        forge-supply@forge-agentic-0821-6418.iam.gserviceaccount.com
forge-workforce     https://forge-workforce-3vnecimj5a-uc.a.run.app     forge-workforce@forge-agentic-0821-6418.iam.gserviceaccount.com

```

Push subscriptions: one per role, single-term filter (`attributes:to_<role>`
— Pub/Sub filters cap at 256 chars, found live), OIDC = the role's own SA,
ordered, DLQ after 5 attempts, retry backoff 60–600s (so redelivery credit
outlives the 120s claim lease). Project-level IAM carries NO pubsub grant on
any forge SA — publisher rights live ON the topic (six members). The
per-minute `forge-tick` heartbeat (Cloud Scheduler, OIDC = orchestrator SA)
runs the ORC-4 monitor, ORC-5 due emission, and the outbox sweep — the
unattended paths RUN as deployed, and dashboard decisions always drain.

## 6. The live spine — NMC to RELEASED on the deployed fleet (Lane 2)

Seven runs total tell the honest story: run 1 BLOCKED (RESERVE_UNAVAILABLE
— every guardrail behaved; root cause was the ungrounded maintenance
planner inventing unstaffable task codes, fixed with the staffable-task
catalog + safety SP-QUAL-001 check); run 2 healthy but the poller deadlocked
on a missed intermediate state (fixed: reached-or-passed semantics); runs
3–4 drove the ordering fixes; run 5 stalled at INTAKE (async push endpoint
vs in-thread asyncio — fixed sync); runs 6 AND 7 PASS end to end. Run 7,
verbatim:

```
LIVE SPINE: project=forge-agentic-0821-6418 workflow=wf-live-105cfddbe9
TRACE (OBS-1 waterfall id): 49f6872f145e532eb94033dcf85c3ec8
dashboard: https://forge-dashboard-3vnecimj5a-uc.a.run.app
nmc_event.v2 published to forge-bus — live workers take it from here
   [02:16:58Z] status=INTAKE
   [02:17:08Z] status=PLANNING
   [02:17:14Z] status=VALIDATING
   [02:17:19Z] status=AWAITING_SCHEDULE_APPROVAL
   decision recorded: {'approval_id': 'apr-49928931-af19-5ef8-8920-ab4c1a636616', 'decision': 'approved', 'approver': 'armond.sinclair@gmail.com'}
   [02:17:22Z] status=AWAITING_SCHEDULE_APPROVAL
   [02:17:27Z] status=SUSPENDED_AWAITING_PART
   due_at (logical day): 126
advancing the Logical Clock 21 simulated days (ORC-5)
   [02:17:28Z] status=SUSPENDED_AWAITING_PART
   [02:17:33Z] status=AWAITING_RELEASE_APPROVAL
   [02:17:34Z] status=AWAITING_RELEASE_APPROVAL
   decision recorded: {'approval_id': 'apr-2e085bed-5e4f-51d1-b919-28ed737a0448', 'decision': 'approved', 'approver': 'armond.sinclair@gmail.com'}
   [02:17:36Z] status=AWAITING_RELEASE_APPROVAL
   [02:17:41Z] status=RELEASED

STATE HISTORY (from the audit trail alone, AUD-2):
   INTAKE
   PLANNING
   VALIDATING
   AWAITING_SCHEDULE_APPROVAL
   SUSPENDED_AWAITING_PART
   ASSEMBLY_RESUMED
   AWAITING_RELEASE_APPROVAL
   RELEASED

ONE trace across 20 audit events: 49f6872f145e532eb94033dcf85c3ec8
Cloud Trace waterfall: https://console.cloud.google.com/traces/list?project=forge-agentic-0821-6418&tid=49f6872f145e532eb94033dcf85c3ec8
LIVE SPINE: PASS
CLEANUP: wf-live-105cfddbe9 records removed; instances re-seeded
exit=0
```

Both decisions were recorded through the DEPLOYED dashboard by the
authenticated principal (armond.sinclair@gmail.com); both HUM-1 gates
consumed their approvals exactly once; the 21-logical-day suspension is
proven from the audit trail alone; the workflow cleaned itself up.

## 7. OBS-1/OBS-2 — one Cloud Trace waterfall, metadata only

Trace `49f6872f145e532eb94033dcf85c3ec8` (run 7): **45 spans under ONE
trace id matching the workflow's trace_id mirror** — every `forge.consume`
hop across all five services PLUS 28 nested ADK spans (invocation →
invoke_agent → call_llm → generate_content) for all 7 live gemini-3.5-flash
calls, including a visible `deduplicated` redelivery. OBS-2 verified on the
exported spans: ADK's content capture is forced OFF
(`ADK_CAPTURE_MESSAGE_CONTENT_IN_SPANS=false`, also set at
`forge_common.otel` import) — every `gcp.vertex.agent.llm_request` /
`llm_response` attribute on the live spans is the empty sentinel `{}`:

```
('call_llm', 'llm_request', 2, '{}')   x7
('call_llm', 'llm_response', 2, '{}')  x7
OBS-2 verdict: CLEAN — every content attribute is the empty sentinel {}
```

forge.consume attributes carry only the OBS-2 metadata set (consumer,
workflow/package/event ids, schema, trace mirror, trust state, outcome);
exception statuses carry the exception TYPE only.

## 8. Pre-freeze HOLD proofs (2026-08-22)

The Day-7 HOLD required three live proofs before freeze. All three pass.

### HOLD-1 — Cyber Trust on deployed infrastructure (SEC-1..4)

Runs 6–7 were the RECOVERY spine (NMC→RELEASED) and did NOT exercise Cyber
Trust — a fair catch. Cyber Trust is now DEPLOYED as `forge-cyber-trust`
(Cloud Run, `--no-allow-unauthenticated`, serving `POST /ingest`), and the
vendor bulletin runs through the deployed service end to end
(`scripts/demo_scene1_live.py`, verbatim):

```
ingest -> HTTP 200: {"verdict_published":true}   (metadata only — SEC-4)
quarantine_verdict.v3 (published to the live bus):
   screening_complete=True raw_disposition=quarantined metadata_release=released
   model_armor=clean classifier=malicious candidate=VND-ACT-9901
Safety veto: vetoed rules=['SP-PART-001', 'SP-SEC-004']
SEC-4 verified: the raw bulletin never left quarantine
```

The path is real: GCS quarantine object + metadata + audit → live Model
Armor → tool-less gemini classifier → non-executable `quarantine_verdict.v3`
on the live bus → deployed Safety worker veto. **Defense-in-depth captured
live**: Model Armor returned `clean` on the full bulletin (the diluted
injection evades it), the classifier caught it (`malicious`/VND-ACT-9901),
and Safety vetoed on the trusted registry — never on the document. The raw
`SYSTEM OVERRIDE` text appears nowhere outside the quarantine record.
(Deploy finding: cyber-trust needed the per-topic publisher grant it now
gets in deploy.sh; the first ingest 500'd on `pubsub.publish` PermissionDenied.)

### HOLD-2 — IAM denial by the platform, not the app (`scripts/iam_matrix_live.py`)

Each identity impersonated; each denied a prohibited op BY GOOGLE IAM, with
a positive control proving the denial is scoping, not a broken credential:

```
[PASS] supply denied bucket WRITE (AGT-5)
[PASS] workforce denied bucket WRITE (AGT-5)
[PASS] supply denied bucket READ (AGT-5)
[PASS] maintenance denied Model Armor (PLT-4): HTTP 403
[PASS] dashboard denied Vertex generateContent: HTTP 403
[PASS] cyber-trust CAN write the bucket   (positive control)
MATRIX: PASS (6/6 cases)
```

### HOLD-3 — one poison, its actual dead-letter arrival

One poison message injected via the regional endpoint; captured on
`forge-bus-dlq` after exactly 5 source deliveries (this is the post-fix
retry policy: 5 attempts over ~5 minutes at 60s backoff, not the earlier
~100ms cascade):

```
injected ONE poison message marker=POISON-a8ca47dc1e19
DEAD-LETTERED on forge-bus-dlq:
   source subscription : forge-bus-orchestrator
   source deliveries   : 5
   original attributes  : to_orchestrator=1 schema=poison.test
LIVE DLQ FORWARDING: PASS (one injected -> one dead-lettered, provenance intact)
```

## 9. Clean-project PLT-6 test (2026-08-22, freeze acceptance step 2)

`deploy.sh` run against a GENUINELY clean project (`forge-clean-08220401`, created +
billing-linked minutes earlier), full chain (SKIP_SETUP=0):

- setup-gcp.sh enabled all APIs, created Firestore, provisioned the Pub/Sub
  service agent, and created the `forge-screening` Model Armor template
  (a completeness gap this test caught — it was smoke-only before);
- the 5-per-minute SA-creation quota returned one HTTP 429 mid-run; the
  script's backoff recovered it (positive evidence the retry works on a
  truly clean project);
- result: **7 role service accounts, all 7 Cloud Run services READY**
  (orchestrator, maintenance, supply, workforce, safety, cyber-trust,
  dashboard), registry loaded with 6 definitions, bus + DLQ + filtered
  push subscriptions + per-minute heartbeat, all from the single command.

deploy.sh exited 0. PLT-6's clean-project verify criterion is met. The
project was deleted immediately afterward to stop billing (the standing
demo project `forge-agentic-0821-6418` remains the acceptance target).
