# GCP smoke verification — Day 1 (2026-08-21)

Project: forge-agentic-0821-6418 · captured 2026-08-21T17:05Z (UTC) · re-runnable via `scripts/smoke_vertex.py` / `scripts/smoke_model_armor.py`

## Vertex AI (PLT-1)

```
model=gemini-3.5-flash region=us response='FORGE-SMOKE-OK'
VERTEX SMOKE: PASS
```

## Model Armor (PLT-4)

Template `forge-screening-smoke` created at `us-central1`; sanitize call on the prompt-injection probe returned:

```
filterMatchState: MATCH_FOUND
pi_and_jailbreak: { executionState: EXECUTION_SUCCESS, matchState: MATCH_FOUND, confidenceLevel: HIGH }
csam:             { executionState: EXECUTION_SUCCESS, matchState: NO_MATCH_FOUND }
```

Both smoke tests exercised the live services on the deployed GCP project (risk R2 retired). Full raw responses are reproducible with the commands above; deployment-tier verification (Cloud Run, IAM, DLQ forwarding, Cloud Trace) is scheduled Day 6–7 (Lane 2, CI-6).

## Google ADK on Vertex (PLT-2)

`PROJECT_ID=forge-agentic-0821-6418 python scripts/smoke_adk_agent.py`
(StructuredAgent → ADK `LlmAgent` + `Runner` → live `gemini-3.5-flash` on
Vertex, location `us`; prompt grounded with the approved-parts excerpt).
Exact captured stdout, 2026-08-21 (stderr carried only SDK deprecation
warnings). Note the grounded model reports the registry's delayed/21-day
facts verbatim — the sourcing_report.v3 semantics:

```
{
  "part_number": "HYD-ACT-4402",
  "part_approved": true,
  "shipment_status": "delayed",
  "eta_days": 21,
  "detail": "Approved hydraulic actuator HYD-ACT-4402 is currently delayed, with an ETA of 21 days."
}
ADK SMOKE: PASS — contract-valid sourcing_report.v3 via ADK Runner on Vertex
```

## Quarantine LIVE COMPONENT smoke (Day 5 corrective, final)

`PROJECT_ID=forge-agentic-0821-6418 python scripts/smoke_quarantine_live.py`
— exact captured stdout, 2026-08-21. This exercises three live components
individually (fresh-ID GCS object with captured generation and a
generation-pinned, SHA-verified read; Model Armor via ADC; the live gemini
classifier). It is NOT the end-to-end pipeline — ingest → screen → outbox →
Safety runs live in Lane 2 (CI-6/CI-8).

```
GCS ROUND-TRIP: PASS — fresh object gs://forge-quarantine-forge-agentic-0821-6418/quarantine/smoke-vsb-637ee36ec7 generation=1787349734145703, metadata raw-free, read generation-pinned
ARMOR (ADC) probe:    {'verdict': 'flagged', 'categories': ['pi_and_jailbreak']}
ARMOR (ADC) bulletin: {'verdict': 'clean', 'categories': []}  <- dilution evades the filter
LIVE CLASSIFIER: label=malicious confidence=1.0 candidate=VND-ACT-9901
QUARANTINE LIVE COMPONENT SMOKE: PASS — armor catches the probe; the classifier catches the dilution (end-to-end pipeline = Lane 2)
```

Notable: the injection diluted in benign vendor prose evades the Model Armor
filter (LOW_AND_ABOVE template) while the pure probe flags — and the live
gemini classifier catches the dilution. Defense in depth is the SEC-1
pipeline's point, demonstrated on live services.
