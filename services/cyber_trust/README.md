# Cyber Trust Agent
Quarantine-first ingestion pipeline: bounded parser -> Model Armor -> tool-less
classifier (SEC-1..3); publishes only structured verdicts + safe metadata
(SEC-4, AGT-5). Fail closed. Lands Day 5.
